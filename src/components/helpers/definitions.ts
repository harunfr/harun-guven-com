export interface NavProps {
  isViewingHeader: boolean;
}
export interface ParallaxProps {
  front?: boolean;
  middle?: boolean;
  back?: boolean;
  verticalMove: number;
  horizontalMove: number;
}
